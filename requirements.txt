# FastAPI 框架及其依赖
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ASGI 服务器，用于运行 FastAPI 应用
# （已包含在 uvicorn[standard] 中，此处列出供参考）

# 邮件发送服务（通过 Resend API）
resend==2.7.0

# HTTP 请求库，用于调用 Webhook 等
requests==2.32.3

# 数据模型与验证库
pydantic==2.8.2
pydantic-settings>=2.0.0

# 从 .env 文件加载环境变量
python-dotenv==1.0.0

# LangChain 相关（LLM 调用）
langchain>=0.3.15
langchain-core>=0.3.85
langchain-community>=0.3.15
langchain-openai>=0.3.15
langchain-anthropic>=0.3.15

# OpenAI SDK（langchain-openai 依赖）
openai>=1.55.0

# Redis（任务队列）
redis==5.2.0

# Markdown 转 HTML（邮件渲染）
mistune>=3.1.0
